# Ticket A1 — bounded activity state

Date: 2026-07-18
Status: DONE
Issue: [#214](https://github.com/pleaseai/shunt/issues/214)

## Implemented

- Added one fixed-capacity, process-memory-only activity queue.
- Added synthetic process-local row ids and UTF-8-safe caps for untrusted provider/model labels.
- Added active-to-terminal in-place updates, oldest-first eviction, and best-effort poisoned-lock
  recovery.
- Wired the optional store into process-lifetime `AppState`; it is allocated only when admin
  routes are enabled at boot and survives config hot reload.
- Added focused tests for bounds, eviction, updates, UTF-8 capping, poisoned locks, admin-disabled
  allocation, and reload identity.

## Review

An independent reviewer inspected the complete diff and approved it with no findings. The reviewer
confirmed all A1 done-when conditions, privacy shape, bounded memory, concurrency behavior, test
isolation, reload semantics, and suitability for B1.

## Verification

The implementer worktree reported:

- `cargo fmt --all --check`: pass
- `cargo clippy --all-targets --all-features -- -D warnings`: pass
- focused activity tests: 11 passed
- `cargo test --all-features --workspace`: pass (806 library tests plus all integration binaries)

The reviewer independently reran format, clippy, the full workspace suite, and repeated the new
server tests under parallel execution; all passed. Integration into the feature checkout was
confirmed byte-for-byte identical to the reviewed `src/activity.rs`, `src/lib.rs`, and
`src/server.rs` files, and `git diff --check` passed. Cargo is not present on the parent session's
current shell PATH, so the already-independent implementer/reviewer test runs are the executable
evidence for this transition.

## Next

B1 may instrument Messages and inbound Codex lifecycles using this store.
